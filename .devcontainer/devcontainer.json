{
  "name": "code-container",
  "dockerFile": "Dockerfile",
  "remoteUser": "dev",
  "mounts": [
    "source=${localEnv:HOME}${localEnv:USERPROFILE}/.gitconfig,target=/home/dev/.gitconfig,type=bind,readonly",
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind,readonly"
  ]
}
